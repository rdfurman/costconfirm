# Docker Environment Configuration Example
# Copy this file to .env and replace all placeholders with actual values
#
# IMPORTANT: .env file should NEVER be committed to version control
# Generate secrets using: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"

# Database Configuration
POSTGRES_USER=costconfirm
POSTGRES_PASSWORD=CHANGE_THIS_TO_RANDOM_STRING
POSTGRES_DB=costconfirm

# DATABASE_URL with connection pool settings (recommended for production)
# - connection_limit: Max number of connections in pool (default: 10)
# - pool_timeout: Seconds to wait for connection from pool (default: 30)
# - connect_timeout: Seconds to wait for initial connection (default: 10)
DATABASE_URL=postgresql://costconfirm:CHANGE_THIS_TO_RANDOM_STRING@db:5432/costconfirm?connection_limit=10&pool_timeout=30&connect_timeout=10

# Auth.js Configuration
# Both NEXTAUTH_SECRET and AUTH_SECRET should have the same value
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
NEXTAUTH_SECRET=CHANGE_THIS_TO_RANDOM_STRING
AUTH_SECRET=CHANGE_THIS_TO_RANDOM_STRING
NEXTAUTH_URL=http://localhost:3000
AUTH_TRUST_HOST=true

# Node Environment
NODE_ENV=development
