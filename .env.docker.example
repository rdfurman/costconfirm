# Docker Environment Configuration Example
# Copy this file to create your own .env.docker if you want to customize
# Note: docker-compose.yml has these values by default

# Database Configuration
POSTGRES_USER=costconfirm
POSTGRES_PASSWORD=costconfirm_dev_password
POSTGRES_DB=costconfirm
DATABASE_URL=postgresql://costconfirm:costconfirm_dev_password@db:5432/costconfirm

# Auth.js Configuration
NEXTAUTH_SECRET=your-super-secret-nextauth-secret-change-this-in-production
NEXTAUTH_URL=http://localhost:3000

# Node Environment
NODE_ENV=development
